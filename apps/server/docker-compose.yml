name: my-better-t-app-2

services:
  postgres:
    image: postgres
    container_name: my-better-t-app-2-postgres
    environment:
      POSTGRES_DB: my-better-t-app-2
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: password
    ports:
      - "5432:5432"
    volumes:
      - my-better-t-app-2_postgres_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U postgres"]
      interval: 10s
      timeout: 5s
      retries: 5
    restart: unless-stopped

volumes:
  my-better-t-app-2_postgres_data: